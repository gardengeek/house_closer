<h2>Listing users</h2>

<table>
  <tr>
    <th>Email</th>
    <th>Name</th>
    <th>Roles</th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%=h user.email %></td>
    <td><%=h user.name %></td>
    <td><%= h(user.roles.collect{|r| r.title}.join(",")) %></td>
    <td><%= link_to 'Show', user %></td>
    <td><%= link_to 'Edit', edit_user_path(user) %></td>
    <td><%= link_to 'Destroy', user, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New user', new_user_path %>
